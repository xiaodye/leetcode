[project]
name = "python-leetcode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.mypy]
strict_optional = false       # 关闭严格可选类型检查
